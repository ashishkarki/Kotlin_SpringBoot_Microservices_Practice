spring:
  profiles:
    active: "development"

service:
  message:
    text: "hello from app properties"

---

spring:
  profiles: "development"

service:
  type: "simple"
  message:
    text: "HI from app properties using development profile"

---

spring:
  profiles: "production"

service:
  type: "advanced"
  message:
    text: "Welcome from app properties using production profile"
